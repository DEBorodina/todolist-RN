import type { Meta } from '@storybook/react';

import { StartScreen } from './StartScreen';

const StartScreenMeta: Meta<typeof StartScreen> = {
  title: 'StartScreen',
  component: StartScreen,
};

export default StartScreenMeta;
