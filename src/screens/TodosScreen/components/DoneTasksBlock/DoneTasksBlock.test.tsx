import React from 'react';

import { render } from '@test-utils';

import { DoneTasksBlock } from './DoneTasksBlock';

describe('DoneTasksBlock', () => {
  it('renders correctly', () => {
    render(<DoneTasksBlock />);
  });
});
